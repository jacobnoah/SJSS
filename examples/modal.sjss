.modal {
  display: none;
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: white;
  padding: 20px;
  border: 1px solid #ccc;

  &.visible {
    display: block;
  }
}

.toggle-btn:click {
  => toggle-class .modal .visible;
}